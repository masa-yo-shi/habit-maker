# Project agents

This directory contains reusable agent prompts for this project.

- `htmx-agent.md`: for UI fragments, htmx behavior, templates, and server-driven HTML.
- `python-agent.md`: for FastAPI, SQLAlchemy, schemas, CRUD logic, and tests.

Use these as task-specific instructions for subagents or manual reviews.
